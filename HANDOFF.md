# TouchComp 项目交接与实施计划

更新日期：2026-07-30

## 1. 项目目标与边界

TouchComp 是从原主从手术机器人项目中拆出的独立验证工程。目标是在改造后的 OpenHaptics Touch 主手上，建立并验证随位置变化的重力/机构偏置补偿；第一阶段不接入 UR、CAN、电机、针机构或六维力传感器。

改造后的机械结构使主手末端朝向始终经过一个固定控制点。Touch 的最后两个转动关节不能输出力矩，因此本项目只处理末端三维位置和三维笛卡尔力，不试图控制或补偿末端姿态力矩。

原则：

- C++/Qt 负责实时主手通信、力输出、安全限幅和数据记录。
- MATLAB 负责离线数据质检、可视化、模型比较和参数导出。
- MATLAB 不进入高频触觉力反馈闭环；即使后续使用 MATLAB Engine/TCP 在线更新参数，最终实时安全输出仍必须在 C++ 内完成。
- 任何缺失数据、异常数值、设备错误、工作空间外位置或程序退出，都应令输出力为零。

## 2. 已确认的几何模型

固定控制点已经精确拟合，可直接使用：

```text
C = (2.5221, 128.7860, 25.9742) mm
```

当前参考球面半径：

```text
r = 174.0 mm
```

用俯仰角 `e`、方位角 `a` 生成球面目标点：

```text
p_target = C + r * [cos(e) cos(a), cos(e) sin(a), sin(e)]
```

采样空间应按该球面的二维角度网格覆盖，而不是在普通 XYZ 立方体内采样。即使补偿模型最后输入 XYZ，角度网格仍能更均匀覆盖实际可达的机械工作面。

## 3. 当前代码与实际状态

当前正式工程只编译下列文件：

| 文件 | 作用 |
| --- | --- |
| `TouchComp/src/main.cpp` | 创建 Qt 程序和采样窗口。 |
| `TouchComp/header/HapticSampler.h` | 主手状态、Button 2 事件和设备生命周期接口。 |
| `TouchComp/src/HapticSampler.cpp` | OpenHaptics 高频回调：读取位置/速度/按钮，始终输出零力。 |
| `TouchComp/header/SamplingWindow.h` | 采样窗口状态。 |
| `TouchComp/src/SamplingWindow.cpp` | 目标网格、到位判定、中文 UI、CSV 写入。 |
| `TouchComp/TouchComp.vcxproj` | 唯一构建清单；只含上述 C++ 源文件。 |

当前版本是“几何采样版”，不是补偿控制版：

- 界面为中文。
- 主手有效输入只有 `Button 2`。
- 高频回调持续输出零力。
- 鼠标按钮“上一目标点 / 下一目标点 / 记录当前点”只用于桌面备用操作。
- 本地未跟踪的旧 UR/CAN/针等文件只是原项目残留，不参与构建，也不应提交到 GitHub。

## 4. 当前几何采样程序如何使用

默认网格为 5 x 5、共 25 点：

- 俯仰角：`-20, -10, 0, 10, 20` 度
- 方位角：`-30, -15, 0, 15, 30` 度
- 到位阈值：目标点三维距离不超过 `2 mm`
- 稳定判据：20 ms 界面周期内连续 25 次到位，即至少约 `0.5 s`

这些常量位于 `TouchComp/src/SamplingWindow.cpp` 顶部。首次现场运行应先确认这组范围没有靠近机械限位；如需扩大范围，先以较小角度增量逐步验证。

现场步骤：

1. 确认原整机程序未占用同一台主手；不要启动或连接 UR、CAN、电机、针机构、力传感器。
2. 启动 TouchComp；若界面提示设备不可用，检查 USB、OpenHaptics 驱动和设备标定。
3. 根据界面目标角度缓慢移动主手。界面实时显示当前位置、到固定点半径和目标误差。
4. 状态显示“已到位”后，继续保持约 0.5 秒。
5. 按主手 `Button 2` 一次。程序记录当前点并自动跳转下一目标点。
6. 完成 25 点后，至少重测中心、四角和两个中间点，作为重复性检查。

输出文件为相对当前工作目录的：

```text
data/geometric_samples.csv
```

列定义：

```text
timestamp,sample_id,target_elevation_deg,target_azimuth_deg,
x_mm,y_mm,z_mm,target_error_mm
```

该文件仅表示几何到位数据，不能被当成补偿力实验数据。

## 5. 已商定的完整实施路线

下面的顺序不可跳过。每阶段的验收通过后，再进入下一阶段。

### 阶段 A：球面几何采样与 MATLAB 质检

目的：验证实际可达空间、目标点到位精度和重复性，不输出任何补偿力。

实施：

1. 使用当前程序完成 25 个点的网格采样。
2. 对中心、四角和两个中间点做至少一次重复采样。
3. 在 MATLAB 中读取 `geometric_samples.csv`，计算：
   - 半径误差 `||p-C|| - r`；
   - 每个目标点的实际误差；
   - 重复采样的均值、标准差和最大偏差；
   - 实际 XYZ 覆盖散点图、俯仰/方位覆盖图。
4. 若某些区域误差大、接近限位或重复性差，缩小/调整网格；不要靠模型“补偿”机械不可重复性。

验收：明确可用球面区域、可接受的几何误差和后续补偿采样的有效点位清单。

### 阶段 B：静态补偿力采样

目的：在阶段 A 验证通过的球面区域内，为每个位置得到抵消重力/机构偏置的三维补偿力 `Fx, Fy, Fz`。

需先实现一个新的采样模式，不能直接拿当前零力版采集：

1. 保持“目标点 + 2 mm + 0.5 s 稳定”的几何门槛。
2. 增加人工可调的候选补偿力输入和明确的软件力使能开关；默认仍为零力。
3. 用很小的力增量逐个调整三个分量，直到操作员松手后无明显单方向漂移、轻推后无明显单向回拉。
4. 每个位置连续记录至少 3 次，记录操作员备注（摩擦、回程、异常等）。
5. 输出 `data/calibration_samples.csv`，建议列为：

```text
timestamp,sample_id,target_elevation_deg,target_azimuth_deg,
x_mm,y_mm,z_mm,fx,fy,fz,repeat_index,operator_note
```

注意：这一步是静态平衡补偿，不要在快速运动时取值，以免把速度相关摩擦误写入静态模型。

### 阶段 C：MATLAB 离线建模与比较

目的：先确认数据能够支持简单、可解释且稳定的模型，避免过早使用神经网络。

按复杂度依次比较：

1. 常量力偏置：只用于验证力方向、坐标符号和安全链路。
2. 最近邻：当前点取最近标定点的补偿力，作为第一版离散模型。
3. 反距离加权（IDW）：利用相邻标定点平滑插值，作为首选初始连续模型。
4. 只有在最近邻/IDW 误差无法接受、且数据覆盖充分时，才评估 RBF 或小型神经网络。

MATLAB 应输出：

- 采样位置三维散点图，按力模长/分量着色；
- 各力分量的球面切片或热图；
- 留一或留点验证误差；
- 工作空间覆盖率与模型有效边界；
- 重复采样误差与异常点清单；
- 固化给 C++ 使用的数据/参数文件。

模型导出建议：优先 CSV + JSON 元数据。JSON 至少包含工作空间边界、插值方法、力限幅、滤波系数、数据集版本；CSV 保存样本位置与三维力。不要让 C++ 依赖 `.mat` 文件。

### 阶段 D：C++ 安全补偿链路

目的：将已经离线验证的模型加载到 C++，但先验证常量力，再启用位置相关补偿。

实时逻辑必须是：

```text
F_model(position)
  -> 工作空间/数据有效性检查
  -> 数值有效性检查
  -> 低通滤波
  -> 每分量及总力限幅
  -> Force Enable 开关
  -> HapticSampler 输出
```

任何失败路径必须直接输出零力：无数据、模型加载失败、位置在有效区外、NaN/Inf、设备错误、关闭窗口、人工禁用。

实施顺序：

1. 常量小力测试，逐轴确认设备坐标方向与界面标注一致。
2. 加入独立的 `Force Enable`，默认关闭；提供明显的 GUI 禁用按钮。
3. 加入保守的每轴/总力限幅与低通滤波。
4. 载入最近邻模型，验证离线预测和实时输出一致。
5. 再替换为 IDW，并在标定工作区外严格归零。

### 阶段 E：补偿效果验证

目的：用记录结果，而非主观手感，判断补偿是否有效。

1. 选定代表性位置（中心、边界、力变化快区域）。
2. 每个位置做补偿关闭/开启的重复试验，顺序应交替或随机，减少操作者适应影响。
3. 记录时间、位置、速度、补偿开关、原始模型力、滤波后力、最终输出力、试验编号。
4. 输出 `data/trial_log.csv`，并用 MATLAB 对比：漂移、维持位置所需的人力、重复性以及是否出现振荡。
5. 只有独立验证有效且未出现安全问题，才考虑把模型复用回原整机项目。

## 6. 安全要求

- 首次任何非零力测试必须从极低限幅开始，并始终保留软件禁用入口。
- Button 2 在当前设计中只用于“记录确认”，不是急停键；急停必须是独立、可靠的控制。
- 当前几何采样版始终零力，因此可用于先验证坐标与网格。
- 不要把关闭 GUI 视为唯一安全措施；未来补偿版本需要明确的生命周期和故障归零测试。
- 切勿在未确认坐标方向、单位与机械限位时提高力值或扩大工作区。

## 7. 构建与跨设备接手

依赖：Visual Studio 2019（C++ 桌面开发与 `v142`）、Qt 5.15.2 MSVC 2019 x64、Qt VS Tools、OpenHaptics Developer 3.5.0 或兼容版本。

每台电脑设置：

```powershell
setx QT_DIR "D:\Qt\5.15.2\msvc2019_64"
setx OPENHAPTICS_DIR "D:\Program Files\OpenHaptics\Developer\3.5.0"
```

重启 Visual Studio/终端后打开 `TouchComp.sln`，选择 `Release | x64` 构建。工程通过 `QT_DIR` 查找 Qt；若项目显示“已卸载”，优先检查 Qt VS Tools、`v142`、Qt MSVC x64 版本和 `QT_DIR` 路径。

构建脚本：

```powershell
powershell -ExecutionPolicy Bypass -File .\scripts\build-release.ps1
```

## 8. Git 与工作区注意事项

- GitHub 主分支是 `main`，远程仓库为 `https://github.com/zzl-wlkq1209/TouchComp.git`。
- 接手电脑已克隆旧分支时，执行：

```powershell
git fetch origin
git switch main
git pull --ff-only origin main
```

- 提交前必须执行 `git status --short`。本地存在未跟踪的旧原项目模块，不要用 `git add -A`，也不要误上传它们。
- 采样产生的 `data/` 文件也应先经过筛选和脱敏/版本标记后再决定是否提交。
